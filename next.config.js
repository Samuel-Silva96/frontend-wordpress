const path = require('path');

module.exports = {
  output: 'standalone', // Gera uma versão otimizada para produção
  typescript: {
    ignoreBuildErrors: false, // Altere para true temporariamente se necessário
  }
}